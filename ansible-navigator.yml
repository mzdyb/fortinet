---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  execution-environment:
    image: hub.rh.lab/ee-network:1.9
    pull:
      policy: missing
  playbook-artifact:
    enable: false
